{
  "version": 2,
  "framework": null,
  "buildCommand": "ls -la && cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "pwd && ls -la",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*\\.(js|css|ico|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot|map))",
      "dest": "/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "functions": {
    "api/*.js": {
      "runtime": "@vercel/node@3"
    }
  }
}
